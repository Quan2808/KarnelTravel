@model Model.TouristSpot

@{
    ViewData["Title"] = "Edit";
}
<section>
    <form asp-action="edit" method="post" enctype="multipart/form-data">
        <input type="hidden" asp-for="ID" />
        <div class="d-flex flex-wrap justify-content-between align-items-center mb-3">
            <div class="d-flex flex-column justify-content-center">
                <h4 class="mb-1 mt-3">Edit Hotel</h4>
            </div>
            <div class="d-flex align-content-center text-center flex-wrap gap-3">
                <button type="button" class="btn btn-danger" id="discard">Discard</button>
                <button type="submit" class="btn btn-primary">Publish</button>
            </div>
        </div>
        <div class="card mb-3">
            <div class="card-header">
                TouristSpot Information
            </div>
            <div class="card-body">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="mb-3">
                    <label class="form-label">Name</label>
                    <input type="text" class="form-control" placeholder="Hotel name" asp-for="Name">
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">Address</label>
                        <input type="text" class="form-control" placeholder="Hotel address" asp-for="Location">
                        <span asp-validation-for="Location" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Hotel</label>
                        <select class="form-control" asp-for="HotelID" asp-items="ViewBag.HotelID">
                            <option value="">No Select</option>
                        </select>
                        <span asp-validation-for="HotelID" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Resort</label>
                        <select class="form-control" asp-for="ResortID" asp-items="ViewBag.ResortID">
                            <option value="">No Select</option>
                        </select>
                        <span asp-validation-for="ResortID" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Restaurant</label>
                        <select class="form-control" asp-for="RestaurantID" asp-items="ViewBag.RestaurantID">
                            <option value="">No Select</option>
                        </select>
                        <span asp-validation-for="RestaurantID" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12 col-lg-8 mb-3">
                <div class="card">
                    <div class="card-header">
                        TouristSpot Description
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <div id="_description">
                                @Html.Raw(Model.Description)
                            </div>
                            <input type="hidden" asp-for="Description" id="Description">
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-12 col-lg-4 mb-3">
                <div class="card">
                    <div class="card-header">
                        Resort Thumbnail
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <div class="mb-4 d-flex justify-content-center">
                                <img id="selectedImage"
                                     src="@Model.Image"
                                     alt="@Model.Name"
                                     style="width: 300px;" />
                            </div>
                            <div class="d-flex justify-content-center">
                                <div class="btn btn-primary btn-rounded">
                                    <label class="form-label text-white m-1" for="thumbnail">Choose file</label>
                                    <input asp-for="Image" type="file" name="imageFile" class="form-control d-none" id="thumbnail" onchange="displaySelectedImage(event, 'selectedImage')" />
                                </div>
                            </div>
                            <span asp-validation-for="Image" class="text-danger"></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
</section>
@section Scripts {
    <script src="https://cdn.ckeditor.com/ckeditor5/40.2.0/classic/ckeditor.js"></script>
    <script src="~/admin/js/site.js" asp-append-version="true"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            document.querySelector('#discard').addEventListener('click', function () {
                Swal.fire({
                    title: "Are you sure?",
                    text: "You won't be able to revert this!",
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#3085d6",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "Yes, delete it!"
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                            title: "Discarded!",
                            text: "The hotel has been discarded.",
                            icon: "success",
                        }).then(() => {
                            window.location.href = '@Url.Action("discard")';
                        });
                    }
                });
            });
        });
    </script>
}
